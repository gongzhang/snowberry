package fatcat.snowberry.tag;


/**
 * 标签监听器。
 * @author 张弓
 * @see IMemberModel#addTagListener(ITagListener)
 * @see IMemberModel#removeTagListener(ITagListener)
 */
public interface ITagListener {
	
	public void tagAdded(ITag tag, IMemberModel memberModel);
	
	public void tagRemoved(ITag tag, IMemberModel memberModel);
	
}
