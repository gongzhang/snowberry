package fatcat.snowberry.tag;


/**
 * 成员模型监听器。
 * @author 张弓
 * @see ITypeModel#addMemberModelListener(IMemberModelListener)
 * @see ITypeModel#removeMemberModelListener(IMemberModelListener)
 */
public interface IMemberModelListener {

	public void memberModelAdded(IMemberModel memberModel);
	
	public void memberModelChanged(IMemberModel memberModel);
	
	public void memberModelRemoved(IMemberModel memberModel);
	
}
